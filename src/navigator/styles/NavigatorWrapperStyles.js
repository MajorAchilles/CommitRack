import { COLORS } from "../../common/enums";

const styles = {
    appContainer: {
        height: "100%",
        width: "100%",
        backgroundColor: COLORS.BACKGROUNDS.WHITE
    }
};

export default styles;